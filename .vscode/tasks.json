{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Restart Dev Server",
      "type": "shell",
      "command": "export PATH=\"/usr/local/bin:$PATH\" && lsof -ti:3000 | xargs kill -9 2>/dev/null || true && npx -y pnpm dev",
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      },
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
